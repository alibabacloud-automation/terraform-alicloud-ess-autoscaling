# Basic Example

This example demonstrates a minimal ESS Auto Scaling configuration with essential parameters.

## Usage

To run this example, execute the following commands:

```bash
terraform init
terraform plan
terraform apply
```

To clean up the resources:

```bash
terraform destroy
```

**Note:** This example will create resources that may incur costs. Make sure to run `terraform destroy` when you are finished to avoid unnecessary charges.
